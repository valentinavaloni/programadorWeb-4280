var items = [22, 30, 28, 25, 24, 44, 40, 25]

console.log('el promedio es ' + result)

function calcAverge(Array) {
  var total = 0
  var age
  for (var i = 0; i < divPromArray.length; i++) {
    divProm = divPromArray[i]
    total = (total + divProm)
  }

  return total
}
